# Browser profiles contain REAL session cookies — never commit or share.
profiles/

# .claude/settings.json IS shipped (shared allow/deny rules). Per-designer overrides are not.
.claude/settings.local.json

tools/node_modules/
.DS_Store
*.preview.png

# API keys for the hosted chat panel. Never committed; on Fly they live as a secret
# (flyctl secrets set), not in the image.
.env
.env.*
!.env.example
